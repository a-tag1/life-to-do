# LifeToDoアプリ システム要件定義・詳細仕様書

## 1. システム概要

日常の「予定」と「実績」をテキストベース（メモ帳感覚）で柔軟に記録・管理するモバイル向けWebアプリケーション（PWA）。
既存のカレンダーアプリのような不自由さを排し、自由なテキスト記述をベースにしながら、「やりたいこと・なりたい像」といった中長期目標の管理、入力補助機能、堅牢なデータ保持対策、習慣管理アプリとの相互連携、および月間・年間での振り返り（★マーク機能）を実現する。

---

## 2. システム構成・動作環境

* **提供形態**: PWA（Progressive Web App） / Webアプリケーション
* **対象端末・OS**: スマートフォン（iOS / Android）の標準ブラウザおよびホーム画面追加後の独立アプリ環境
* **UI/UX仕様**:
* モバイルファーストのレスポンシブデザイン（片手操作を意識）
* ダークモード対応（OS設定連動、または手動切り替え）
* オフライン動作（Service Workerによるキャッシュ利用）


* **データ保存構造**: クライアントサイドストレージ（IndexedDB / LocalStorage）を主軸とし、データ消失防止対策を実装

---

## 3. 機能要件・詳細仕様

### 3.1. ビジョン・目標管理機能

* **やりたいこと・なりたい像（ビジョン）の登録**:
* 定期的に見返したい「なりたい姿」や「やりたいこと（Bucket List）」をテキスト入力・保持する領域を提供。


* **具体行動・タスク管理**:
* ビジョンに紐づく具体的なアクション（タスク）をリスト形式で管理。
* **完了フラグ機能**: チェックボックス等により「完了 / 未完了」をワンタップで切替可能。
* **当日予定への転送機能**: タスク内容を「当日スケジュール」の予定入力欄へワンタップでコピー・追加挿入できるボタンを配置。



### 3.2. 予定・実績記録機能（当日スケジュール画面）

* **テキスト自由記述領域**:
* 「予定」エリアと「実績」エリアを同一画面内に分離して配置。
* 各エリアは改行やスペースを自由に使えるテキストエリア形式。
* *記述例：*
* `予定：` `今：10:00` `読書：10:00-10:30` `ピアノ：～10:50`
* `実績：` `読書：10:00～10:20` `ピアノ：～10:30`




* **入力補助機能**:
* **「今」ボタン（タイムスタンプ挿入）**:
* 現在時刻を取得し、**5分単位で切り上げて**テキストのカーソル位置に挿入（フォーマット例: `14:15`）。
* 例：`14:11` または `14:13` にボタンを押下した場合 ➔ `14:15` を挿入。


* **テンプレート挿入（定型文機能）**:
* 毎日使う定型文（例：`読書：` `運動：`）のボタンを配置し、ワンタップでカーソル位置へ挿入。
* テンプレート（定型文）の一覧・内容は、ユーザーが自由に追加・修正・削除可能。





### 3.3. スケジュール表示と移動（今月・年間）

* **今月スケジュール画面**:
* 該当月の日付を縦並び（リスト形式）で一覧表示。
* 各日付ごとに、**予定・実績を区別しない同一のメモ枠**を提供（自由記述）。
* 各日付の横に「★（スター）マーク」のオン/オフ切替ボタンを設置。


* **年間スケジュール画面**:
* 12ヶ月分（1月〜12月）のグリッド/リスト表示。
* 各月ごとにメモ領域を提供。
* **重要日ハイライト（★マーク抽出表示）**: 今月スケジュールで「★マーク」が付与された特定日付のメモのみを抽出し、年間画面上にピックアップ表示する。


* **画面ナビゲーション・操作**:
* **スワイプ移動**: 画面上の左右スワイプ操作で、前日/翌日（当日画面）、前月/翌月（今月画面）、前年/翌年（年間画面）へ直感的に遷移。
* **「今日へ戻る」ボタン（FAB: Floating Action Button）**: 過去や未来の日付・年月を閲覧中、ワンタップで「本日の当日スケジュール画面」へ即座に復帰。



### 3.4. 外部習慣アプリ（Habit Tracker）連携

* **相互アプリ間移動導線**:
* メイン画面等に「習慣アプリを開く」ボタンを常設。
* タップ時に指定のURL（またはカスタムURLスキーム）を開き、別アプリとして起動。
* （※習慣アプリ側にもLifeToDoのPWA URLを開く戻りリンクを設定することを推奨運用とする）



### 3.5. データ消失防止・バックアップ対策

PWAのキャッシュクリア（OSのストレージクリーンアップ）によるデータ損失リスクを回避するため、以下の多重対策を実装する。

1. **永続化ストレージ要求（Persistent Storage）**:
* アプリ起動時に `navigator.storage.persist()` を自動呼び出しし、OSによる自動削除対象からアプリデータを保護。


2. **クラウド自動バックアップ（Google Drive / Dropbox API等）**:
* ユーザー個人のクラウドストレージ領域（App Data folder等）へデータを同期・自動保存。


3. **Web Share API による外部送信**:
* ワンタップでOS標準の「共有（Share）シート」を呼び出し、LINEの自分用メモ帳や純正メモ機能へデータを直接テキスト転送。


4. **テキストファイル出力（エクスポート）**:
* 蓄積されたデータを `.txt` または `.csv` 形式のテキストファイルとして手動ダウンロード保存可能。



---

## 4. 画面遷移・画面構成

```text
                        【外部習慣アプリ】
                                ▲
                                │ (相互リンク / 別アプリ起動)
                                ▼
                       ┌─────────────────┐
                       │  [1] メイン画面  │
                       │ (当日スケジュール)│
                       └────────┬────────┘
                                │
   ┌───────────────┬────────────┼────────────┬───────────────┐
   │               │            │            │               │
   ▼               ▼            ▼            ▼               ▼
┌──────┐       ┌──────┐     ┌──────┐     ┌──────┐     ┌──────────────┐
│ [2]  │       │ [3]  │     │ [4]  │     │ [5]  │     │ (ダイアログ) │
│ 今月 │       │ 年間 │     │ビジョン│    │ 設定 │     │ テンプレート │
│スケ  │       │スケ  │     │ ・   │     │  ・  │     │   挿入       │
│ジュール│     │ジュール│    │ 行動 │     │バックアップ│   └──────────────┘
└──────┘       └──────┘     └──────┘     └──────────────┘

```

* **[1] メイン画面（当日スケジュール）**:
* 毎日アクセスする基本画面。「予定」「実績」入力領域、タイムスタンプ（5分切り上げ）ボタン、テンプレート呼び出し、今月/ビジョン/設定への遷移ボタンを配置。


* **[2] 今月スケジュール画面**:
* 日付縦並びリスト。各日のメモ入力および★マークの付与。


* **[3] 年間スケジュール画面**:
* 12ヶ月のメモ表示および★マーク日付の抽出表示。


* **[4] ビジョン・目標管理画面**:
* 「やりたいこと・なりたい像」の入力・管理、具体アクションタスクのチェックリスト管理および当日予定への転送機能。


* **[5] 設定・バックアップ画面**:
* 定型文（テンプレート）のカスタマイズ、Google Drive等のクラウド連携設定、手動エクスポート（テキスト出力/Web Share）。



---